/** Loader metadata for the standalone DSH bundle. */
/** Stable Cordis plugin name used by cordis.patch.yml. */
export const name = "@deepseek-ai/dsh-skill-state";
/** Required services supplied by every profile that runs the plugin. */
export const inject = ["tools", "systemPrompt"];
